# ===========================================
# ESP32 T-Series Fan — Secrets Example
# ===========================================
# Copy file này thành secrets.yaml và điền
# thông tin WiFi của bạn.
# secrets.yaml đã được gitignore, KHÔNG commit.
# ===========================================

# WiFi credentials
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
